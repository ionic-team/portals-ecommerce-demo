platform :ios, '14.0'
use_frameworks!

def portals_pods
  pod 'IonicPortals', '~> 0.10.0'
  pod 'CapacitorCamera', '~> 5.0'
end

target 'Ecommerce' do
  portals_pods
end

target 'EcommerceLiveUpdates' do
  portals_pods
end

target 'EcommerceTests' do
  portals_pods
end
