platform :ios, '14.0'
use_frameworks!

def portals_pods
  pod 'Capacitor'
  pod 'IonicPortals', '~> 0.6.0'
  pod "CapacitorCamera"
end

target 'Ecommerce' do
  portals_pods
end

target 'EcommerceLiveUpdates' do
  portals_pods
end

target 'EcommerceTests' do
  portals_pods
end
